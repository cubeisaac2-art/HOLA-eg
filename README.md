# HOLA GUINEA

A Flask-based Progressive Web App for Equatorial Guinea.

## Getting started

1. Create a virtual environment.
2. Install dependencies: `pip install -r requirements.txt`
3. Run the app: `python app.py`

## Project status

This project is in early scaffolding phase. The initial config, dependency list, and folder structure were created to support the full application build.
